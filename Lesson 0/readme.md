1. setup tools: vs code, chrome browser
2. html overview

- elements and tags
- doctype
- html
- p with "hello world"

3. head section

- title
- meta utf-8
- link to stylesheet
- add hello class .hello color red, add class to p tag

4.  browser resets

- global css reset
- meta viewport for mobile

5. javascript

- create script.js file
- add script tag to body
- reload page

6. button click to call javascript

- add defer to script tag
  Download this file in parallel with HTML
  Do not execute it yet
  Wait until the HTML is fully parsed
  Then execute it, in order

- add get element by id and event listener javascript code

7. input element

- add input element with id nameInput
- add button element with id showName
- add name input and name button element id
- add name input and name button event listeners

8. strings vs numbers

- A
- add input and button for number
- same js
- click button we see the string number
- B
- add second number input and button
- add them together
- we have a bug! the numbers are concatenated as strings
- we need to convert them
- strings vs numbers data types
- C
- convert input values to numbers using the built-in Number function

9. console.log

- “console.log prints values to the browser’s developer console so we can see what JavaScript is actually doing.”
